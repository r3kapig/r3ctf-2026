#!/bin/sh
# lift is a static reverse-engineering challenge (pure attachment).
# There is no image to build and no remote service to deploy.
set -e
echo "lift: static attachment -> attachment/chall (players reverse it offline)"
echo "See README.md for details."
